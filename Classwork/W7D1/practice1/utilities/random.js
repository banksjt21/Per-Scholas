

module.exports.getRandom = function(min, max) {
    return parseInt(Number(Math.random() * (max - min) + min).toFixed()); 
}





module.exports.getRandom2 = function(min, max) {
    return parseInt(Number(Math.random() * (max - min) + min).toFixed()); 
}
